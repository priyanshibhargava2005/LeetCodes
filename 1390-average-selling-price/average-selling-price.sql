# Write your MySQL query statement below
select p.product_id,round(ifnull(sum(s.units*p.price)/sum(s.units),0),2) as average_price from Prices p left join UnitsSold s on p.product_id=s.product_id and purchase_date between start_date and end_date group by p.product_id;